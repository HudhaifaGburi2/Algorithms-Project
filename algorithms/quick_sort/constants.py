"""
Quick Sort specific constants.
No functions allowed in constants files.
"""

# Default array for demonstrations (R3: Deterministic)
DEFAULT_ARRAY = [7, 2, 9, 4, 3, 8, 5]

# Pivot selection strategies
PIVOT_LAST = "last"
PIVOT_FIRST = "first"
PIVOT_MEDIAN = "median"

# Default pivot strategy
DEFAULT_PIVOT_STRATEGY = PIVOT_LAST

# Recursion visualization limits
MAX_RECURSION_DEPTH_DISPLAY = 4
MIN_SUBARRAY_SIZE_DISPLAY = 1

# Animation timing multipliers
PARTITION_SPEED_MULTIPLIER = 1.0
COMPARISON_SPEED_MULTIPLIER = 0.8
SWAP_SPEED_MULTIPLIER = 1.2
